package com.example.demo.common.enums;

/**
 * 请求包类型
 */
public enum PacketageEnum {
    HTTP("HTTP"),
    HTTP_S("HTTPs"),
    DNS("DNS");

    private String packType;

    PacketageEnum(String dns) {

    }
}
